apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "asset.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "asset.name" . }}
    helm.sh/chart: {{ include "asset.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "asset.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
