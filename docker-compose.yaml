version: '3.3'

services:
  postput_api:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env
    environment:
      SEQUELIZE_FORCE_SYNC: "true"
    ports:
      - '2000:3000'
